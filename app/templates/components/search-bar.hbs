{{input value=query placeholder=placeholder}}

{{#if results}}
  <ul class="styled lift">
    {{#each results as |result|}}
      <li>
        {{link-to result.title "datasets" result.id}}
      </li>
    {{/each}}
  </ul>
{{/if}}
